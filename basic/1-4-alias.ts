{
  // type alias

  type Text = string;
  const name: Text = "mark";

  type Student = {
    name: string;
    age: number;
  };

  const stu: Student = {
    name: "mark",
    age: 21,
  };

  // string literal Types
  type word = "word";
  const dong: word = "word";

  type bool = true;
  const tru: bool = true;
}
